export const EXPORT_REPORT_TYPES = {
  ATTENDANCE: 'attendance',
  FACULTY: 'faculty',
  STUDENTS: 'students',
  DEVICES: 'devices',
  SYNCHRONIZATION: 'synchronization',
};

export const EXPORT_REPORT_TYPES_VALUES = Object.values(EXPORT_REPORT_TYPES);

export const EXPORT_FORMATS = {
  CSV: 'csv',
  XLSX: 'xlsx',
  PDF: 'pdf',
};

export const EXPORT_FORMATS_VALUES = Object.values(EXPORT_FORMATS);

export const EXPORT_STATUS = {
  PENDING: 'PENDING',
  SUCCESS: 'SUCCESS',
  FAILED: 'FAILED',
};

export const EXPORT_STATUS_VALUES = Object.values(EXPORT_STATUS);
