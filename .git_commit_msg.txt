feat: implement device management module

- Add Device Management module
- Implement device CRUD operations
- Add device validation and structured logging
- Support fingerprint, face recognition, and hybrid devices
- Add pagination, filtering, sorting, and search
- Implement soft delete with audit fields
- Add device status management endpoint
- Keep module device-agnostic and production-ready
